'use strict';
function handleClick(button) {
  button.onclick = function() {
    openMail();
  };
  button.onclick = function() {
    openMail();
  };
}

const button = document.getElementById('open');
handleClick(button);